"use client";

import { IEvent } from "@/lib/database/models/event.model";
import { formatDateTime } from "@/lib/utils";

type ClientCardProps = {
  event: IEvent;
};

const ClientCard = ({ event }: ClientCardProps) => {};
